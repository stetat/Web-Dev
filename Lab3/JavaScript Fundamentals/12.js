/*
function checkAge(age) {
    if (age > 18) {
        return true;
    } else {
        // ...
        return confirm('Did parents allow you?');
    }
}
Will the function work differently if else is removed?

    function checkAge(age) {
        if (age > 18) {
            return true;
        }
        // ...
        return confirm('Did parents allow you?');
        Yes, the function would work;
    }

function checkAge(age) {
  (age > 18) ? true : confirm("Did parens allow you?");
}

function checkAge(age) {
  (age > 18) || confirm("Did parens allow you?");
}

function min(a, b) {
    return (a < b) ? a : b;
}

function pow(x, n) {
    return x ** n;
}




    */
